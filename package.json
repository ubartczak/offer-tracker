{
  "name": "offer-tracker",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:ext": "pnpm --filter extension dev",
    "build": "pnpm --filter api build && pnpm --filter web build && pnpm --filter extension build"
  }
}